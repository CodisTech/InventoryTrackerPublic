# To commit and push the changes to your repository, run these commands:

# Add all the modified files to staging
git add Dockerfile docker-compose.yml docker-entrypoint.sh setup-db.js

# Commit the changes with the message from commit_message.txt
git commit -F commit_message.txt

# Push to your repository (adjust branch name if needed)
git push origin main